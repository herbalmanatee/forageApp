import React from 'react';
import styled from 'styled-components'

let ExplorePage = (props) => {
  return(
    <div>This is the page for exploring the network of users</div>
  )
};

export default ExplorePage;
