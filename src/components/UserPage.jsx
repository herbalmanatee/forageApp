import React from 'react';
import styled from 'styled-components';

let UserPage = (props) => {
  return(
    <div>This is the page with a specific users information.
      Here they can input new locations on the map.
      Here they can view their friends and their friends maps as well.
      Navigation to the Explore Page will also be available.
    </div>
  )
}

export default UserPage;